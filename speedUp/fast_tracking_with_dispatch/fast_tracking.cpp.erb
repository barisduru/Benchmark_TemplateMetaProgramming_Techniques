#include "utility_fast.hpp"

#include <type_traits>


template<int>
struct S;

using input = list<<%= (0..(n/2-1)).map{ |i| "S<#{i}>" }.join(', ').to_s + ", void, " + ((n/2+1)..(n-2)).map{ |i| "S<#{i}>" }.join(', ') + ", void" %>>;


#if defined (METABENCH)

  using <%= "result#{n}" %> = remove_if_t<std::is_void,input>;

#endif


int main() {}

